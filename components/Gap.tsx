import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Gap = ({ height = 0 }) => {
  return (
    <View style={{
         height: height
    }}></View>
  )
}

export default Gap
