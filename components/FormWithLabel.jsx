import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const FormWithLabel = ({ height = 0 }) => {
  return (
    <View style={{
         height: height
    }}></View>
  )
}

export default FormWithLabel;
